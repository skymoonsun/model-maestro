version: '3.8'

services:
  ollama-proxy:
    build: .
    container_name: ollama-proxy
    network_mode: "host"
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

